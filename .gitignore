node_modules/
.DS_Store
_to_delete/

# raw audio masters stay local — the game ships compressed versions
sound/*.wav
!sound/UIclick.wav

# 🌍 eternal-ledger credentials and local ledger fallbacks stay OUT of the public repo
global_leaderboard.json
leaderboard.json
nation_stats.json

# the original full-size paintings stay local — the game ships 1600px JPEGs
art/*.png
